package io.cloudsmith.example;

/** Minimal app; the point is exercising Cloudsmith Maven download + upload. */
public final class App {
    private App() {}

    public static void main(String[] args) {
        System.out.println("cloudsmith-maven-oidc-demo built and resolved its Cloudsmith dependency");
    }
}
